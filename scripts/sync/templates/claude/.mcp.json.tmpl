{
  "_generated": "{{header}}",
  "mcpServers": {
    "rtl-harness": {
      "command": "uv",
      "args": ["run", "--project", "{{harness_dir}}", "--no-sync", "python", "{{harness_dir}}/mcp/server.py"]
    }
  }
}
